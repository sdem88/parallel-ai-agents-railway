{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && npm run build:all"
  },
  "deploy": {
    "startCommand": "echo 'Please deploy individual services'",
    "healthcheckTimeout": 300,
    "restartPolicyType": "on_failure"
  }
}